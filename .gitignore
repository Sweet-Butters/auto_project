__pycache__/
*.py[cod]
*$py.class
*.so

.venv/
venv/
env/

build/
dist/
*.egg-info/
*.egg

.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

.env
.env.local

# secret-bearing files — never commit
keys.txt
*credentials*
*secret*
*.key
*.pem

# local notes / transcripts (not part of the framework)
httpswww.youtube.com*.txt
*.transcript.txt

# private projects living as nested repos under this folder
# (each has its own .git pointing to a private GitHub repo).
# Leading slash anchors to repo root so this doesn't accidentally match
# `src/auto_project/` etc.
/*_project/

.vscode/
.idea/
*.swp
.DS_Store
